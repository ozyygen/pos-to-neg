# Family-disjoint_properties.ipynb
- We filtered the discovered positive rules with confidence scores.
- The code first filters the rules based on confidence score >0.85. Then calculates specificity per rule.
